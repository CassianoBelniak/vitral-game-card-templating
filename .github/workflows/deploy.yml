name: Deploy

on:
    workflow_call:
    workflow_dispatch:

permissions:
    contents: write
    pull-requests: write

jobs:
    deploy-windows:
        runs-on: windows-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'

            - name: Install dependencies
              run: npm ci

            - name: Update version in package.json
              run: |
                  npm version ${GITHUB_REF_NAME}

            - name: Build project
              run: npm run build
              env:
                  GH_TOKEN: ${{ secrets.DEPLOY_TOKEN }}

            - name: Upload windows installer
              env:
                  GITHUB_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
              run: gh release upload ${{ steps.release.outputs.tag_name }} ./dist/vitral-windows-installer.exe

            - name: Upload windows portable
              env:
                  GITHUB_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
              run: gh release upload ${{ steps.release.outputs.tag_name }} ./dist/vitral-windows-portable.exe

    deploy-linux:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'

            - name: Install dependencies
              run: npm ci

            - name: Build project
              run: npm run build

            - name: Upload linux package
              env:
                  GITHUB_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
              run: gh release upload ${{ steps.release.outputs.tag_name }} ./dist/vitral-linux.deb
